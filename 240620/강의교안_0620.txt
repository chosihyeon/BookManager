CREATE TABLE BOOK (
   NO NUMBER PRIMARY KEY,
    BOOKID VARCHAR2(64) NOT NULL UNIQUE,
   TITLE VARCHAR2(64) NOT NULL,
   AUTHOR VARCHAR2(64) NOT NULL,
   PUBLISHER VARCHAR2(64) NOT NULL,
   PRICE NUMBER NOT NULL,
    REGDATE DATE NOT NULL
);

--시퀀스 생성
CREATE SEQUENCE BOOK_SEQ 
 START WITH 1 INCREMENT BY 1 MAXVALUE 99999 NOCYCLE NOCACHE NOORDER;
 
--시퀀스 삭제
DROP SEQUENCE BOOK_SEQ;
  
--시퀀스 확인
SELECT *
  FROM USER_SEQUENCES;

--시퀀스 가장 최근 번호 확인
SELECT BOOK_SEQ.CURRVAL
  FROM DUAL;
  
-- 테이블 정보 확인
SELECT *
  FROM USER_CONSTRAINTS WHERE TABLE_NAME = 'BOOK'; 

--도서 추가
INSERT INTO BOOK (NO, BOOKID, TITLE, AUTHOR, PUBLISHER, PRICE, REGDATE)
VALUES (BOOK_SEQ.NEXTVAL, 'BOOKID001', 'TITLE001', 'AUTHOR001', 'PUBLISHER001', 24800, SYSDATE);

--도서 삭제
DELETE BOOK 
 WHERE NO = 1;

--도서 정보 수정
UPDATE BOOK
   SET PASSWORD = 'UPDATE_PWD', NICKNAME = 'UPDATE_NICK'
 WHERE NO = 2;

--도서 조회
SELECT *
  FROM BOOK;

SELECT *
  FROM BOOK
 WHERE NO = 1;
